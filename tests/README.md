# Tests

- `unit/` is for fast, isolated tests of pure logic and small modules.
- `integration/` is for tests that cross boundaries such as APIs, storage, or multiple modules working together.
- End-to-end coverage belongs in `../e2e/`.
